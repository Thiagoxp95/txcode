{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "HOST",
    "PORT",
    "VITE_WS_URL",
    "VITE_DEV_SERVER_URL",
    "TXCODE_LOG_WS_EVENTS",
    "TXCODE_MODE",
    "TXCODE_PORT",
    "TXCODE_NO_BROWSER",
    "TXCODE_HOME",
    "TXCODE_AUTH_TOKEN",
    "TXCODE_DESKTOP_WS_URL",
    "TXCODE_TRACE_MIN_LEVEL",
    "TXCODE_TRACE_TIMING_ENABLED",
    "TXCODE_TRACE_FILE",
    "TXCODE_TRACE_MAX_BYTES",
    "TXCODE_TRACE_MAX_FILES",
    "TXCODE_TRACE_BATCH_WINDOW_MS",
    "TXCODE_OTLP_TRACES_URL",
    "TXCODE_OTLP_METRICS_URL",
    "TXCODE_OTLP_EXPORT_INTERVAL_MS",
    "TXCODE_OTLP_SERVICE_NAME"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "dist-electron/**"]
    },
    "dev": {
      "dependsOn": ["@t3tools/contracts#build"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": [],
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": []
    }
  }
}
